# -*- coding: utf-8 -*-
#
# Copyright (C) 2025 CERN.
# Copyright (C) 2025 KTH Royal Institute of Technology.
#
# Invenio-Checks is free software; you can redistribute it and/or modify it
# under the terms of the MIT License; see LICENSE file for more details.

# TODO: Transifex integration
#
# 1) Create message catalog:
#    $ pybabel extract -F pyproject.toml -o invenio_checks/translations/messages.pot invenio_checks
#    $ pybabel init -i invenio_checks/translations/messages.pot -d invenio_checks/translations -l <lang>
#    $ pybabel compile -d invenio_checks/translations --use-fuzzy
# 2) Ensure project has been created on Transifex under the inveniosoftware
#    organisation.
# 3) Install the transifex-client
#    $ curl -o- https://raw.githubusercontent.com/transifex/cli/master/install.sh | bash
# 4) Push source (.pot) and translations (.po) to Transifex
#    $ tx push -s -t
# 5) Pull translations for a single language from Transifex
#    $ tx pull -l <lang>
# 6) Pull translations for all languages from Transifex
#    $ tx pull -a

[main]
host = https://app.transifex.com

[o:inveniosoftware:p:invenio:r:invenio-checks-messages]
file_filter = invenio_checks/translations/<lang>/LC_MESSAGES/messages.po
source_file = invenio_checks/translations/messages.pot
source_lang = en
type = PO
